git add .
git commit -m "Resolved conflicts"
git push -u origin main


git pull --rebase origin main
# Los eventuele mergeconflicten op als ze opduiken
# Daarna:
git push -u origin main

git stash
git pull --rebase origin main
git stash pop


git push -f origin main

git pull

git add . && git commit -m "Update van Stark" && git push