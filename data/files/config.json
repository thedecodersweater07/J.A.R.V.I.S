{
    "data_sources": {
        "training": "training_data.csv",
        "test": "test_data.csv"
    },
    "model_settings": {
        "batch_size": 32,
        "learning_rate": 0.001,
        "epochs": 100
    }
}
